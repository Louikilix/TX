<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <% if @writing_definition.present? && (controller.controller_name == "writing_definitions" || controller.controller_name == "writing_images") && controller.action_name == "show" %>
      <%= link_to "TX", edit_writing_definition_url(@writing_definition),  {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window', class:"navbar-brand"}  %>
    <% else %>
      <%= link_to "TX", root_path, class:"navbar-brand" %>
    <%end%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="t" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
            <% if @writing_definition.present? && (controller.controller_name == "writing_definitions" || controller.controller_name == "writing_images") && controller.action_name == "show" %>
              <%= link_to "Créations", edit_writing_definition_url(@writing_definition),  {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window', class:"nav-link"}  %>
            <% else %>
              <%= link_to "Créations", home_index_path, class:"nav-link" %>
            <%end%>
        </li>
      </ul>
    </div>
  </div>
</nav>
